import React from 'react'
import ReactDOM from 'react-dom/client'
import LeaveManagementDashboard from './leave-modal'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LeaveManagementDashboard />
  </React.StrictMode>,
)
